doctype html
html
    head
        include _head.pug
    body
        include _header.pug
        .ts.stackable.grid
            include _nav.pug
            .twelve.wide.column
                +bigheader('開始使用','從這裡開始吧')
                +header('引用','下載並解壓縮後，保持資料夾結構，丟到你的網頁目錄，即可開始使用！')
                +code
                    :markdown-it(linkify langPrefix='highlight-')
                        ```html
                        <!-- Chat UI-->
                        <link href="/path/to/css/ChatUI.css" rel="stylesheet">
                        ```
                +header('外部引用','不放在自家伺服器的方法')
                +code
                    :markdown-it(linkify langPrefix='highlight-')
                        ```html
                        <!-- Chat UI-->
                        <link href="https://earlyspringcommitee.github.io/ChatUI/css/ChatUI.css" rel="stylesheet">
                        ```
                +header('範例','連結在下面，自己戳戳')
                a(href='https://earlyspringcommitee.github.io/ChatUI/test/testMsgr.html') Msgr
                br
                a(href='https://earlyspringcommitee.github.io/ChatUI/test/test.html') Telegram
                +bigheader('基本元素','')
                +header('聊天室','搭拉，就像這樣')
                +header('訊息','')
                +header('圖片訊息','')
                +header('單一圖片訊息','')
                +header('影片訊息','')
                +header('單一影片訊息','')
                +header('嵌入網頁訊息','')
                +header('單一嵌入網頁訊息','')
                +bigheader('樣式','')
                +header('樣式','')
                +bigheader('Messenger','')
                +header('Messenger 無法使用的元素','')
                +bigheader('Telegram','')
                +header('Telegram 無法使用的元素','')
        
        include _footer.pug